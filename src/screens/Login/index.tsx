import React from 'react';
import { Text } from 'react-native';

import { LoginWrapper } from './styles';
import { ILoginProps } from './types';

const Login = (props: ILoginProps) => {
  return (
    <LoginWrapper>
      <Text>Que pasa DROPPPEY</Text>
    </LoginWrapper>
  );
};

export default Login;
